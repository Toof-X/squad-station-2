# Squad Station — Claude orchestrator + Claude workers
# Copy to project root as squad.yml, then run: squad-station init

project: my-project

sdd:
  - name: get-shit-done
    playbook: ".squad/sdd/gsd-playbook.md"
  # - name: bmad-method
  #   playbook: ".squad/sdd/bmad-playbook.md"
  # - name: superpowers
  #   playbook: ".squad/sdd/superpowers-playbook.md"

orchestrator:
  provider: claude-code
  role: orchestrator
  model: haiku
  description: Product Owner, Strategic Advisor & Squad Coordinator — decides priorities, delegates all execution to agents, monitors progress, synthesizes results. Never executes tasks directly.

agents:
  - name: implement
    provider: claude-code
    role: worker
    model: sonnet
    description: Senior coder, coding, fixing bugs

  - name: brainstorm
    provider: claude-code
    role: worker
    model: opus
    description: Technical Lead, planner, analysis, code reviews
