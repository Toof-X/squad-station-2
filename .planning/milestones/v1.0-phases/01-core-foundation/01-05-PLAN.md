---
phase: 01-core-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - tests/helpers.rs
  - tests/test_db.rs
  - tests/test_commands.rs
  - src/tmux.rs
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - MSG-01
  - MSG-02
  - MSG-03
  - MSG-04
  - MSG-05
  - MSG-06
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04

must_haves:
  truths:
    - "All DB operations tested against real SQLite (in-memory or tempfile)"
    - "Message CRUD operations verified: insert, update status, list with filters, peek with priority ordering"
    - "Idempotency verified: duplicate insert_agent is no-op, duplicate signal update returns 0 rows"
    - "tmux arg vectors verified: send-keys always includes -l, launch uses direct command"
    - "Binary compiles and runs: `cargo build` succeeds, `squad-station --help` shows all commands"
    - "cargo test passes with zero failures"
  artifacts:
    - path: "tests/helpers.rs"
      provides: "Shared test infrastructure: in-memory SQLite pool setup with migrations"
      min_lines: 15
    - path: "tests/test_db.rs"
      provides: "DB layer tests: agent CRUD, message CRUD, idempotency, priority ordering"
      min_lines: 80
    - path: "tests/test_commands.rs"
      provides: "Command logic tests: config parsing, DB path resolution"
      min_lines: 30
  key_links:
    - from: "tests/test_db.rs"
      to: "src/db/agents.rs"
      via: "insert_agent, get_agent, list_agents"
      pattern: "db::agents::"
    - from: "tests/test_db.rs"
      to: "src/db/messages.rs"
      via: "insert_message, update_status, list_messages, peek_message"
      pattern: "db::messages::"
    - from: "tests/test_db.rs"
      to: "src/db/mod.rs"
      via: "connect() with test DB"
      pattern: "db::connect"
---

<objective>
Create comprehensive tests for the DB layer, command logic, and safety primitives. Verify all 12 Phase 1 requirements have working automated checks.

Purpose: Without tests, we cannot verify correctness of messaging, idempotency, and safety invariants. Tests also serve as the verification gate for the entire phase.
Output: Passing test suite covering all DB operations, idempotency guarantees, and safety invariant checks.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-foundation/01-CONTEXT.md
@.planning/phases/01-core-foundation/01-RESEARCH.md
@.planning/phases/01-core-foundation/01-01-SUMMARY.md
@.planning/phases/01-core-foundation/01-02-SUMMARY.md
@.planning/phases/01-core-foundation/01-03-SUMMARY.md
@.planning/phases/01-core-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test infrastructure and DB layer tests</name>
  <files>tests/helpers.rs, tests/test_db.rs</files>
  <action>
**tests/helpers.rs:**
- Create a shared helper module for integration tests
- `pub async fn setup_test_db() -> sqlx::SqlitePool` — creates an in-memory SQLite pool with WAL mode and runs migrations:
  ```rust
  use sqlx::sqlite::{SqliteConnectOptions, SqliteJournalMode, SqlitePoolOptions};
  let opts = SqliteConnectOptions::new()
      .filename(":memory:")  // or use tempfile for file-based tests
      .create_if_missing(true)
      .journal_mode(SqliteJournalMode::Wal);
  let pool = SqlitePoolOptions::new()
      .max_connections(1)
      .connect_with(opts).await.unwrap();
  sqlx::migrate!("./src/db/migrations").run(&pool).await.unwrap();
  pool
  ```
- Note: In-memory SQLite may not fully support WAL mode — if it fails, use `tempfile::NamedTempFile` for a real file-based test DB instead

**tests/test_db.rs:**
- `mod helpers;` at top to import shared helpers

**Agent CRUD tests:**
- `test_insert_and_get_agent`: insert agent, get by name, verify all fields match
- `test_insert_agent_idempotent`: insert same agent name twice, verify no error (INSERT OR IGNORE), get_agent returns the first insertion
- `test_list_agents`: insert 3 agents, list all, verify count = 3 and ordered by name
- `test_get_nonexistent_agent`: get_agent for unknown name returns None

**Message CRUD tests:**
- `test_insert_message`: insert message for agent, verify returned id is valid UUID
- `test_insert_message_with_priority`: insert with "high" priority, list and verify priority field
- `test_update_status_completes_message` (MSG-02): insert message, update_status, verify status changed to "completed"
- `test_update_status_idempotent` (MSG-03): insert message, update_status twice, second call returns 0 rows_affected — no error
- `test_update_status_no_pending` (MSG-03): call update_status when no pending messages exist, returns 0 rows — not an error

**List filter tests (MSG-04):**
- `test_list_filter_by_agent`: insert messages for 2 agents, list with agent filter, verify only matching messages returned
- `test_list_filter_by_status`: insert messages, complete some, list with status="pending", verify filtering
- `test_list_with_limit`: insert 10 messages, list with limit=3, verify only 3 returned
- `test_list_no_filters`: list all messages, verify all returned

**Peek tests (MSG-06):**
- `test_peek_returns_pending`: insert 2 messages (one pending, one completed), peek returns only the pending one
- `test_peek_priority_ordering` (MSG-05): insert 3 pending messages with different priorities (normal, high, urgent), peek returns the urgent one
- `test_peek_no_pending`: peek for agent with no pending messages returns None
- `test_peek_nonexistent_agent`: peek for unknown agent returns None (not error)

All tests use `#[tokio::test]` attribute and the `setup_test_db()` helper.
Each test is independent — creates its own DB pool.
  </action>
  <verify>
    <automated>cd /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station && cargo test --test test_db 2>&1</automated>
  </verify>
  <done>All DB layer tests pass. Agent CRUD verified. Message insert/update/list/peek verified. Idempotency for both agents (INSERT OR IGNORE) and signals (UPDATE WHERE status='pending') verified. Priority ordering in peek verified.</done>
</task>

<task type="auto">
  <name>Task 2: Create command logic tests, verify tmux safety, and run full suite</name>
  <files>tests/test_commands.rs, src/tmux.rs</files>
  <action>
**tests/test_commands.rs:**
- `mod helpers;` at top

**Config parsing tests:**
- `test_config_parse_valid_yaml`: create a valid squad.yml string, deserialize with serde_saphyr, verify SquadConfig fields. Use inline YAML string (not file) to keep test self-contained:
  ```rust
  let yaml = r#"
  project:
    name: test-squad
  orchestrator:
    name: test-orchestrator
    provider: claude-code
    role: orchestrator
    command: "claude --dangerously-skip-permissions"
  agents:
    - name: frontend
      provider: claude-code
      role: worker
      command: "claude --dangerously-skip-permissions"
  "#;
  let config: SquadConfig = serde_saphyr::from_str(yaml).unwrap();
  assert_eq!(config.project.name, "test-squad");
  assert_eq!(config.agents.len(), 1);
  ```
- `test_config_parse_missing_fields`: YAML with missing required fields should return error, not panic
- `test_db_path_resolution_default`: config with no db_path should resolve to `~/.agentic-squad/{project}/station.db`
- `test_db_path_resolution_custom`: config with `db_path: "/custom/path.db"` uses that path

**tmux safety tests (verify in src/tmux.rs #[cfg(test)] module):**
- If not already present from Plan 01, ensure these unit tests exist in `src/tmux.rs`:
- `test_send_keys_args_include_literal_flag` (SAFE-02): verify the args vector includes `-l`
- `test_enter_args_no_literal_flag`: verify Enter is sent without -l
- `test_launch_args_use_direct_command` (SAFE-03): verify new-session args include the command directly
- These test the extracted arg-building helper functions, not actual tmux invocation

**SIGPIPE test (SAFE-04):**
- `test_sigpipe_binary_starts`: run `cargo run -- --help` as a subprocess, verify it exits 0 and shows help text. This implicitly verifies SIGPIPE handler doesn't crash the binary.

**Full suite run:**
- After all tests written, run `cargo test` to verify everything passes together
- Run `cargo build` to verify the binary compiles
- Run `cargo run -- --help` to verify CLI works end-to-end

**Fix any compilation or test issues** discovered during the full suite run. This task is the phase gate — everything must be green.
  </action>
  <verify>
    <automated>cd /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station && cargo test 2>&1</automated>
  </verify>
  <done>All tests pass: DB layer, config parsing, tmux safety invariants, SIGPIPE handler. `cargo build` succeeds. `cargo run -- --help` shows all 6 subcommands. Full test suite green — phase gate passed.</done>
</task>

</tasks>

<verification>
- `cargo test` passes with zero failures
- `cargo build` produces working binary
- `cargo run -- --help` shows: init, send, signal, list, peek, register
- All 12 requirement IDs have at least one test covering them:
  - SESS-01: config parsing + DB creation tests
  - SESS-02: insert_agent idempotency test
  - MSG-01: insert_message test
  - MSG-02: update_status test
  - MSG-03: idempotency tests (duplicate insert, duplicate signal)
  - MSG-04: list filter tests
  - MSG-05: priority ordering test
  - MSG-06: peek tests
  - SAFE-01: WAL mode configured in test DB setup
  - SAFE-02: tmux arg vector tests
  - SAFE-03: launch arg vector tests
  - SAFE-04: binary startup test
</verification>

<success_criteria>
- `cargo test` passes with all tests green
- `cargo build` succeeds with zero warnings (or only expected deprecation warnings)
- Every Phase 1 requirement has at least one automated test
- No panics, no SQLite busy errors, no compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation/01-05-SUMMARY.md`
</output>
