pub async fn run(name: String, command: String, role: String, provider: String, json: bool) -> anyhow::Result<()> {
    let _ = (name, command, role, provider, json);
    todo!("implement in plan 02")
}
